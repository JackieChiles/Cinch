#!/usr/bin/python3
"""
A collection of functions for parsing Cinch game events into usable data.

Current contents:

process(gid, hid): Grab all events with the chosen game id (gid) and hand
                   number (hid) and parse them for addition to the other tables.

"""

import sqlite3
from datetime import datetime, timezone

def process(gid, hid):
    pass # pretty hard-hitting stuff!
